import React, { useState } from "react";
import Layout from "../components/Layout";
import NavMobile from "../components/NavMobile";

function projects() {
  return (
    <Layout>
      <NavMobile /> projects page
    </Layout>
  );
}

export default projects;
