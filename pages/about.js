import React, { useState } from "react";
import Layout from "../components/Layout";
import NavMobile from "../components/NavMobile";
const about = () => {
  return (
    <Layout>
      <NavMobile /> about page
    </Layout>
  );
};

export default about;
